<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Schedules Management</title>
</head>
<body>
    <%- include('../partials/header') %>
    <br>
    <div class="mydiv">
     <% if(schedules.length==0){ %>
         <p>No schedules for the user</p>
     <% }else{ %>
        <% for(let i=0;i<schedules.length;i++){ %>
            <div class="innerdiv">
                <p>Schedule id:<%= schedules[i].scheduleid %></p>
                <p>User id:<%= schedules[i].userid %></p>
                <p>Name:<%= schedules[i].firstname %> <%= schedules[i].lastname %>(<%= schedules[i].firstname.charAt(0).toUpperCase() %><%= schedules[i].lastname.charAt(0).toUpperCase() %>)</p>
                <p>Day:<%= schedules[i].sday %></p>
                <p>Start At:<%= schedules[i].start_at %></p>
                <p>End At:<%= schedules[i].end_at %></p>
            </div>
            <%} %>
        <%} %>
    
    </div>   
</body>
</html>